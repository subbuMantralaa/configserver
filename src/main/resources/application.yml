server:
  port: 8071

spring:
  application:
    name: configserver

  profiles:
    active: native
    #active: git

  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config
    #git
     #uri:
      #default-label: main
      #timeout: 5
      #clone-on-start: true